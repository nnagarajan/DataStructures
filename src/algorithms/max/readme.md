Algorithm to find max occurrence of a character in a string
 
  Example input => outputs
  
     * “aaabcdef” => ‘a’
     * “aabb”  => [exception]
     *  null  => [exception]
     

Algorithm finds the max occurrence with O(n) complexity     
     
Groovy and JUnit included